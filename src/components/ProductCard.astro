---
import type { Product } from '../lib/catalog'
import { specLine } from '../lib/commerce'

export interface Props { product: Product; loading?: 'lazy' | 'eager'; showCategory?: boolean }
const { product: p, loading = 'lazy', showCategory = false } = Astro.props
const img = p.images[0]
---
<article class="card card-hover">
  <a class="card-link" href={p.url}>
    <div class="card-media">
      {img ? (
        <img
          src={`${img.base}-400.webp`}
          srcset={`${img.base}-400.webp 400w, ${img.base}-800.webp 800w`}
          sizes="(max-width: 620px) 92vw, (max-width: 1024px) 45vw, 300px"
          width={img.width} height={img.height}
          loading={loading} decoding="async"
          alt={`${p.name} shown as a finished custom-printed pack`}
        />
      ) : <div class="ph" aria-hidden="true"><span>{p.name}</span></div>}
    </div>
    <div class="card-body">
      <p class="card-spec">{specLine(p)}</p>
      <h3 class="card-title">{p.name}</h3>
      {showCategory && <p class="card-text">{p.category}</p>}
      <span class="card-foot">
        Specify this
        <svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><path d="M5 12h13M13 6l6 6-6 6"/></svg>
      </span>
    </div>
  </a>
</article>
<style>
  .ph { display:grid; place-items:center; height:100%; background:var(--bone-deep);
    color:var(--ink-4); font-size:var(--fs-sm); padding:var(--s-4); text-align:center; }
</style>
